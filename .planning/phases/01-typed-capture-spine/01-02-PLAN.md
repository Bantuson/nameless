---
phase: 01-typed-capture-spine
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - crates/nameless-core/src/lib.rs
  - crates/nameless-core/src/fragment.rs
  - crates/nameless-core/src/provenance.rs
  - crates/nameless-core/src/state_machine.rs
autonomous: true
requirements: [CAP-05]
must_haves:
  truths:
    - "The fragment lifecycle is one typed enum covering the human path (captured→analyzing→analyzed→placed→mixed→rendered) and the ai path (requested→generating→generated→evaluating→{promoted|rejected})."
    - "`transition(state, transition)` is a single exhaustive `match` returning `Result<FragmentState, IllegalTransition>` — there is no second write path that mutates state."
    - "Placing a fragment that has not reached `analyzed` returns `Err(IllegalTransition)`; placing from `analyzed` returns `Ok(Placed)` — enforced by the harness (the match), not by convention."
    - "An ai_generated fragment can only reach `placed` via `promoted` (the eval-gate path); `generated → placed` directly is an illegal transition."
    - "Provenance is the complete typed set: human_recorded | ai_generated | derived | sampled (sampled defined now, used in Phase 8)."
  artifacts:
    - path: "crates/nameless-core/src/state_machine.rs"
      provides: "FragmentState + Transition enums + the exhaustive checked transition function + IllegalTransition error"
      contains: "fn transition"
    - path: "crates/nameless-core/src/provenance.rs"
      provides: "Provenance enum (4 variants incl. sampled)"
      contains: "enum Provenance"
  key_links:
    - from: "crates/nameless-core/src/fragment.rs"
      to: "crates/nameless-core/src/state_machine.rs"
      via: "Fragment.state is FragmentState; mutations go only through transition()"
      pattern: "FragmentState"
    - from: "crates/nameless-core/src/state_machine.rs"
      to: "crates/nameless-core/src/provenance.rs"
      via: "transition guards branch on Provenance (ai path requires eval gate; sampled/human do not)"
      pattern: "Provenance::"
  verification_classes:
    ram_safe_toolchain_gated:
      - "cargo test -p nameless-core (exhaustive transition matrix + 'cannot place unanalyzed' invariant). Pure-logic, in-memory, no DB — designed to fit 4GB. Requires rustup (absent on dev machine)."
    env_gated:
      - "(none — this plan is pure domain logic)"
---

<objective>
Replace the minimal placeholder state/provenance from Plan 01 with the **complete typed fragment lifecycle** and the checked transition function that structurally enforces the phase's headline invariant: **an unanalyzed fragment can never be placed, and an ungated ai_generated fragment can never enter an arrangement** — enforced by an exhaustive Rust `match`, not by convention (PRD §7, CONTEXT State Machine decision).

Phase 1 wires the live `captured` entry transition only; this plan DEFINES and EXHAUSTIVELY TESTS the whole lifecycle so the invariant is locked early and cheaply. It does NOT implement feature extraction, the analyzed-driving worker, or any `analyze`/`place` CLI command — those are Phase 2+ (deferred per CONTEXT). Defining a transition's target state is not the same as implementing the worker that drives it.

Purpose: lock "the harness gates, the agent explores" at the type level on day one (expensive to retrofit).
Output: `provenance.rs`, `state_machine.rs`, an exhaustive transition test matrix proving the invariant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-typed-capture-spine/01-CONTEXT.md
@nameless-prd.md
@.planning/research/ARCHITECTURE.md
@crates/nameless-core/src/fragment.rs
@crates/nameless-core/src/ports.rs

# This plan is PURE domain logic in nameless-core — no heavy deps, no DB, fully RAM-safe to verify (once toolchain installed).
# It MODIFIES fragment.rs/lib.rs from Plan 01; it depends on Plan 01 and runs in Wave 2 (no file overlap with Plans 03/04).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete provenance + lifecycle enums + the checked transition function</name>
  <files>crates/nameless-core/src/provenance.rs, crates/nameless-core/src/state_machine.rs, crates/nameless-core/src/fragment.rs, crates/nameless-core/src/lib.rs</files>
  <read_first>
    - nameless-prd.md §7 (the two lifecycle paths; "the eval gate is the only path from generated to placed; there is no bypass")
    - .planning/phases/01-typed-capture-spine/01-CONTEXT.md (State Machine decision: complete typed model now, place() only from Analyzed, exhaustive match → Result<_, IllegalTransition>)
    - .planning/research/ARCHITECTURE.md (Pattern 3 illustrative match: sampled/human vs ai-gated placement)
    - crates/nameless-core/src/fragment.rs (the minimal placeholder to replace)
  </read_first>
  <behavior>
    Define expected transitions BEFORE wiring the match (these become the test matrix in Task 2):
    - Human/sampled path: Captured --Analyze--> Analyzing --Analyzed--> Analyzed --Place--> Placed --Mix--> Mixed --Render--> Rendered.
    - AI path: Requested --Generate--> Generating --Complete--> Generated --Evaluate--> Evaluating --Promote--> Promoted --Place--> Placed (then Mix/Render); Evaluating --Reject--> Rejected (terminal).
    - Invariant 1 (CAP-05 headline): Place is legal ONLY from Analyzed (human/sampled) or from Promoted (ai). Place from Captured/Analyzing/Generated/Evaluating/Requested/Generating → Err(IllegalTransition).
    - Invariant 2: there is no direct Generated→Placed edge (eval gate is the only path).
    - Every other (state, transition) pair → Err(IllegalTransition) with the offending pair captured in the error.
  </behavior>
  <action>
    In `provenance.rs`: `enum Provenance { HumanRecorded, AiGenerated, Derived, Sampled }` (serde rename_all = snake_case; `sampled` included now per CONTEXT even though sampling ships in Phase 8). Replace the Plan-01 minimal `Provenance` reference in `fragment.rs` with this.

    In `state_machine.rs`:
    - `enum FragmentState { Captured, Analyzing, Analyzed, Placed, Mixed, Rendered, Requested, Generating, Generated, Evaluating, Promoted, Rejected }` (serde snake_case; derive Debug/Clone/Copy/PartialEq/Eq).
    - `enum Transition { Analyze, MarkAnalyzed, Place, Mix, Render, Generate, MarkGenerated, Evaluate, Promote, Reject }`.
    - `struct IllegalTransition { from: FragmentState, transition: Transition }` via `thiserror` (Display: "illegal transition: cannot apply {transition:?} from state {from:?}").
    - `fn transition(provenance: Provenance, from: FragmentState, t: Transition) -> Result<FragmentState, IllegalTransition>` implemented as ONE exhaustive `match` over `(provenance, from, t)` (or `(from, t)` with a provenance guard on Place/Generate-family). The compiler's exhaustiveness check is the enforcement: every legal edge returns `Ok(next)`, the catch-all returns `Err(IllegalTransition{from, transition:t})`. Place must require `from == Analyzed` (human/sampled/derived) or `from == Promoted` (ai). Use exhaustive arms — do NOT use a permissive wildcard that could silently allow an unintended edge; the wildcard only maps to the error.
    - Provide `Fragment::apply(&mut self, t: Transition) -> Result<(), IllegalTransition>` that calls `transition(self.provenance, self.state, t)` and assigns on Ok — making `transition()` the SINGLE chokepoint for state mutation (no other code sets `state` directly).
    - Update `lib.rs` to `pub mod provenance; pub mod state_machine;` and re-export.

    Keep this crate dependency-light (serde, uuid, thiserror only) — no new deps.
  </action>
  <verify>
    <automated>cargo test -p nameless-core</automated>
    <env_gate>RAM-SAFE but TOOLCHAIN-GATED: requires rustup (absent on the dev machine). Pure-logic core crate, no DB/heavy deps — compiles + runs the transition tests within 4GB.</env_gate>
  </verify>
  <acceptance_criteria>
    - `FragmentState`, `Transition`, `IllegalTransition`, `Provenance`, `fn transition`, `Fragment::apply` are public from `nameless_core`.
    - `transition` compiles with exhaustive matching (no non-error wildcard that swallows legal edges).
    - `Fragment.state` is the only state field and is mutated solely through `apply`/`transition` (grep the crate: no `self.state =` outside `apply`).
  </acceptance_criteria>
  <done>The complete lifecycle + provenance enums and the single checked transition function compile in nameless-core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exhaustive transition test matrix proving the "cannot place unanalyzed" invariant</name>
  <files>crates/nameless-core/src/state_machine.rs</files>
  <read_first>
    - crates/nameless-core/src/state_machine.rs (the transition function from Task 1)
    - nameless-prd.md §7 (legal edges)
  </read_first>
  <behavior>
    The test module iterates the full cartesian product of (Provenance × FragmentState × Transition) and asserts each result against an explicit expected-legal table. Legal edges → Ok(expected_next); every other combination → Err(IllegalTransition) with from/transition echoed. Dedicated named tests pin the headline invariants so a regression names itself.
  </behavior>
  <action>
    Add a `#[cfg(test)] mod tests` in `state_machine.rs`:
    - `fn legal_edges() -> &'static [(Provenance, FragmentState, Transition, FragmentState)]` — the hand-written allow-list of every legal edge (the spec).
    - `test_full_transition_matrix`: loop over all `Provenance::ALL × FragmentState::ALL × Transition::ALL`; if the triple is in `legal_edges` assert `transition(..) == Ok(next)`, else assert it is `Err`. (Define `ALL` const arrays for each enum.)
    - `test_cannot_place_unanalyzed`: assert `transition(HumanRecorded, Captured, Place).is_err()`, `(_, Analyzing, Place).is_err()`, and `transition(HumanRecorded, Analyzed, Place) == Ok(Placed)`.
    - `test_ai_requires_eval_gate`: assert `transition(AiGenerated, Generated, Place).is_err()` (no bypass) and the only route to Placed is `... Evaluating --Promote--> Promoted --Place--> Placed`.
    - `test_sampled_travels_human_path`: `transition(Sampled, Analyzed, Place) == Ok(Placed)` (sampled uses the human lifecycle; the attribution gate that further restricts it is Phase 8, NOT here).
    - `test_rejected_is_terminal`: no transition out of `Rejected`.
    - `test_illegal_transition_reports_pair`: the error carries the offending `from`+`transition`.
  </action>
  <verify>
    <automated>cargo test -p nameless-core state_machine</automated>
    <env_gate>RAM-SAFE but TOOLCHAIN-GATED: requires rustup (absent here). Pure unit tests, no DB — this is the harness-level proof of success criterion #3. Fits 4GB.</env_gate>
  </verify>
  <acceptance_criteria>
    - `test_full_transition_matrix` passes (every triple matches the legal-edge allow-list; all others are errors).
    - `test_cannot_place_unanalyzed` passes — Place from Captured/Analyzing is `Err`, from Analyzed is `Ok(Placed)`.
    - `test_ai_requires_eval_gate` passes — `Generated→Place` is `Err`; placement only via Promoted.
    - `test_rejected_is_terminal` and `test_illegal_transition_reports_pair` pass.
  </acceptance_criteria>
  <done>The exhaustive matrix + invariant tests pass, proving the state machine refuses to place unanalyzed (or ungated) work at the harness level.</done>
</task>

</tasks>

<artifacts_produced>
- **Files:** `crates/nameless-core/src/provenance.rs`, `crates/nameless-core/src/state_machine.rs` (modifies `fragment.rs`, `lib.rs`).
- **Types:** `Provenance` (HumanRecorded|AiGenerated|Derived|Sampled), `FragmentState` (12 states), `Transition` (10 transitions), `IllegalTransition`.
- **Functions:** `transition(provenance, from, transition) -> Result<FragmentState, IllegalTransition>`, `Fragment::apply`.
- **Consumed by:** Plan 04 (Postgres repo maps `FragmentState`/`Provenance` to enum columns); Phase 2 (drives Captured→Analyzed); Phase 8 (adds the sampled attribution gate on Place).
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| agent/caller → state mutation | Any code attempting to change a fragment's lifecycle state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | placing an unanalyzed/ungated fragment | mitigate | `Place` legal only from Analyzed (human/sampled/derived) or Promoted (ai); exhaustive match, no bypass edge; proven by `test_cannot_place_unanalyzed` + `test_ai_requires_eval_gate`. |
| T-02-02 | Tampering | alternate state-write path | mitigate | `transition()` is the single chokepoint; `Fragment::apply` is the only mutator; grep-gate that no `self.state =` exists elsewhere. |
| T-02-03 | Repudiation | silent illegal transition | mitigate | Illegal transitions return a typed `IllegalTransition{from, transition}` error (not a silent no-op), so callers must handle it. |
</threat_model>

<verification>
**RAM-safe (toolchain-gated, pure-logic core — install rustup, then runs within 4GB):**
- `cargo test -p nameless-core` (the full transition matrix + invariant tests).

**Env-gated:** none (no DB, no network, no heavy deps).
</verification>

<success_criteria>
- The complete typed lifecycle compiles; the exhaustive test matrix passes; "cannot place unanalyzed" and "ai needs the eval gate" are enforced by the `match` and proven by named tests. CAP-05 (structural state machine) satisfied at the harness level.
</success_criteria>

<output>
Create `.planning/phases/01-typed-capture-spine/01-02-SUMMARY.md` when done.
</output>
