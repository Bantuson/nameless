---
phase: 01-typed-capture-spine
plan: 04
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - crates/nameless-adapters/Cargo.toml
  - crates/nameless-adapters/src/lib.rs
  - crates/nameless-adapters/src/repo_pg.rs
  - crates/nameless-adapters/src/queue_sqlxmq.rs
  - crates/nameless-adapters/src/object_store_s3.rs
  - crates/nameless-cli/Cargo.toml
  - crates/nameless-cli/src/profile.rs
  - migrations/0001_init.sql
  - .env.example
autonomous: true
requirements: [CAP-02, CAP-07]
user_setup:
  - service: postgres
    why: "Production FragmentRepo + durable sqlxmq queue. No psql/Docker on the 4GB dev machine → this whole plan is ENV-GATED: built here, verified by the user against a real Postgres 16/17 + pgvector."
    env_vars:
      - name: DATABASE_URL
        source: "postgres://USER:PASS@HOST:5432/nameless (Neon, a local PG, or any managed PG)"
    dashboard_config:
      - task: "Create a Postgres 16/17 database; run `sqlx migrate run` (or `cargo run -- migrate`) to apply migrations/0001_init.sql"
        location: "User's real environment (not the dev machine)"
  - service: s3-r2
    why: "Production immutable object storage by content-hash (CAP-02). Filesystem fake (Plan 01) is the local stand-in."
    env_vars:
      - name: NAMELESS_STORAGE_BUCKET
        source: "Cloudflare R2 (or any S3-compatible) bucket name"
      - name: NAMELESS_STORAGE_ENDPOINT
        source: "R2 S3 API endpoint URL"
      - name: NAMELESS_STORAGE_ACCESS_KEY_ID
        source: "R2 API token (access key id)"
      - name: NAMELESS_STORAGE_SECRET_ACCESS_KEY
        source: "R2 API token (secret)"
must_haves:
  truths:
    - "Behind a non-default `postgres` feature, `PostgresFragmentRepo` implements `FragmentRepo` against Postgres via sqlx (compile-time-checked queries)."
    - "`SqlxmqJobQueue` implements `JobQueue` on a Postgres-backed sqlxmq queue that survives process restart, retries on failure, and applies backpressure — no NATS/Redis."
    - "`S3ObjectStore` implements `ObjectStore` storing raw audio immutably by content-hash key in S3/R2."
    - "The default build remains lean: enabling these requires `--features postgres`; default `cargo build` pulls NO tokio/sqlx/sqlxmq."
    - "`migrations/0001_init.sql` creates the projects + fragments tables and the provenance + fragment_state enum types matching the Rust enums."
  artifacts:
    - path: "migrations/0001_init.sql"
      provides: "Initial Postgres schema (projects, fragments, provenance + state enum types)"
      contains: "create table fragments"
    - path: "crates/nameless-adapters/src/repo_pg.rs"
      provides: "PostgresFragmentRepo (sqlx)"
      contains: "PostgresFragmentRepo"
    - path: "crates/nameless-adapters/src/queue_sqlxmq.rs"
      provides: "SqlxmqJobQueue (durable Postgres queue)"
      contains: "SqlxmqJobQueue"
    - path: "crates/nameless-adapters/src/object_store_s3.rs"
      provides: "S3ObjectStore (R2-compatible, content-hash key)"
      contains: "S3ObjectStore"
  key_links:
    - from: "crates/nameless-adapters/src/repo_pg.rs"
      to: "migrations/0001_init.sql"
      via: "sqlx queries map Fragment/Project to the projects/fragments tables + enum types"
      pattern: "fragments"
    - from: "crates/nameless-cli/src/profile.rs"
      to: "crates/nameless-adapters/src/repo_pg.rs"
      via: "non-local profile builds PostgresFragmentRepo + SqlxmqJobQueue + S3ObjectStore from env"
      pattern: "PostgresFragmentRepo"
  verification_classes:
    ram_safe_toolchain_gated:
      - "cargo build -p nameless-adapters (DEFAULT features) must still succeed WITHOUT tokio/sqlx in the tree — proves the feature gate keeps the lean build intact. Requires rustup (absent on dev machine)."
    env_gated:
      - "cargo build -p nameless-adapters --features postgres  (heavy: tokio+sqlx+sqlxmq — MAY OOM on 4GB; run on a real dev box)."
      - "DATABASE_URL=... cargo sqlx migrate run  (apply schema to a live Postgres)."
      - "DATABASE_URL=... cargo test -p nameless-adapters --features postgres -- --ignored  (PostgresFragmentRepo round-trip + sqlxmq durability-across-restart, against live Postgres)."
      - "NAMELESS_STORAGE_*=... cargo test ... --features postgres s3 -- --ignored  (S3/R2 put/get by content hash, against a real bucket)."
---

<objective>
Add the **production heavy leaf** behind a non-default `postgres` feature so the spine has its real backends without breaking the RAM-safe default build: `PostgresFragmentRepo` (sqlx, compile-time-checked SQL), `SqlxmqJobQueue` (durable Postgres-backed queue — survives restart, retries, backpressure, no NATS/Redis), and `S3ObjectStore` (immutable by content-hash in S3/R2). Plus the initial migration DDL.

Per ENVIRONMENT.md, this plan is **built here, verified in the user's real environment**: there is no `psql`/Docker and only ~4GB RAM, and the `postgres`-feature compile (tokio+sqlx+sqlxmq) may OOM on the dev machine. The deliverable is real, reviewed, idiomatic code; every `<verify>` step that needs Postgres/S3/the heavy compile is explicitly env-gated with the exact command. The ONLY thing verified on the dev machine is that the DEFAULT build stays lean (the feature gate works).

Purpose: complete CAP-02 (immutable S3/R2 by-ID) and CAP-07 (durable sqlxmq) production paths while preserving the 4GB-buildable default.
Output: `migrations/0001_init.sql`, `repo_pg.rs`, `queue_sqlxmq.rs`, `object_store_s3.rs`, server profile wiring, `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-typed-capture-spine/01-CONTEXT.md
@.planning/ENVIRONMENT.md
@nameless-prd.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@crates/nameless-core/src/ports.rs
@crates/nameless-core/src/state_machine.rs
@crates/nameless-core/src/job.rs

# ENV-GATED PLAN. Heavy deps (tokio, sqlx 0.8.6, sqlxmq, S3 client) live ONLY behind the `postgres` feature.
# Default `cargo build` must NOT pull them. The dev machine cannot run the postgres-feature build or any live
# Postgres/S3 test — give the EXACT command the user runs in their real environment for each.
# Crate legitimacy for these deps was approved in Plan 01 Task 0 (the full-phase supply-chain checkpoint).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initial migration DDL + PostgresFragmentRepo (sqlx) behind the postgres feature</name>
  <files>migrations/0001_init.sql, crates/nameless-adapters/Cargo.toml, crates/nameless-adapters/src/repo_pg.rs, crates/nameless-adapters/src/lib.rs</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (Data Model Deltas — projects/fragments shape; provenance enum)
    - nameless-prd.md §6 (core tables)
    - crates/nameless-core/src/state_machine.rs + provenance.rs (the enum variants the DB enum types must match)
    - .planning/research/STACK.md (sqlx 0.8.6, compile-time-checked SQL; Postgres 16/17 + pgvector 0.8)
  </read_first>
  <action>
    In `nameless-adapters/Cargo.toml` add a `[features] postgres = ["dep:tokio", "dep:sqlx", "dep:sqlxmq"]` (deps marked `optional = true`): `tokio` (rt-multi-thread, macros), `sqlx` 0.8.6 (`runtime-tokio`, `postgres`, `uuid`, `chrono`, `macros`). Keep them OUT of the default feature set so the lean build is unchanged.

    `migrations/0001_init.sql`:
    - `create type provenance as enum ('human_recorded','ai_generated','derived','sampled');`
    - `create type fragment_state as enum ('captured','analyzing','analyzed','placed','mixed','rendered','requested','generating','generated','evaluating','promoted','rejected');` (must match `FragmentState`/`Provenance` exactly).
    - `create table projects (id uuid primary key, title text not null, created_at_ms bigint not null);`
    - `create table fragments (id uuid primary key, project_id uuid not null references projects(id), kind text not null, provenance provenance not null, audio_uri text not null, duration_ms int, sample_rate int, note_text text not null, state fragment_state not null, parent_fragment_id uuid references fragments(id), created_at_ms bigint not null);`
    - Enable pgvector for later (`create extension if not exists vector;`) but DO NOT add embedding columns now (Phase 2 owns features/embeddings — do not pre-build deferred work).

    `repo_pg.rs` (gated `#[cfg(feature = "postgres")]`): `PostgresFragmentRepo { pool: PgPool }` implementing the `FragmentRepo` trait (async internally; bridge to the sync trait via the chosen pattern — e.g. an async-capable trait variant or `pool` + blocking shim; document the choice). Use sqlx `query!`/`query_as!` (compile-time-checked) for insert_project/insert_fragment/list_fragments/get_fragment. Map `FragmentKind` to text, `Provenance`/`FragmentState` to the enum types. Mark live-DB tests `#[ignore]` (run only with DATABASE_URL).
  </action>
  <verify>
    <automated>cargo build -p nameless-adapters</automated>
    <env_gate>RAM-SAFE check (toolchain-gated): the DEFAULT build must still succeed and must NOT pull tokio/sqlx (`cargo tree -p nameless-adapters` shows neither). ENV-GATED (user's real env, NOT the 4GB dev box): `cargo build -p nameless-adapters --features postgres` (heavy — may OOM on 4GB); then `DATABASE_URL=postgres://... cargo sqlx migrate run`; then `DATABASE_URL=... cargo test -p nameless-adapters --features postgres -- --ignored` for the repo round-trip against live Postgres.</env_gate>
  </verify>
  <acceptance_criteria>
    - Default `cargo build -p nameless-adapters` succeeds with NO tokio/sqlx in the dependency tree (feature gate works) — the one dev-machine-checkable assertion.
    - `migrations/0001_init.sql` enum types enumerate exactly the Rust `Provenance` (4) and `FragmentState` (12) variants.
    - `PostgresFragmentRepo` implements `FragmentRepo` (compiles under `--features postgres`).
    - (Env-gated) repo round-trip test against a live Postgres inserts + reads back a project and fragment.
  </acceptance_criteria>
  <done>The schema + sqlx repo exist behind the `postgres` feature; the default build stays lean.</done>
</task>

<task type="auto">
  <name>Task 2: SqlxmqJobQueue (durable queue) + S3ObjectStore + server profile wiring</name>
  <files>crates/nameless-adapters/Cargo.toml, crates/nameless-adapters/src/queue_sqlxmq.rs, crates/nameless-adapters/src/object_store_s3.rs, crates/nameless-adapters/src/lib.rs, crates/nameless-cli/Cargo.toml, crates/nameless-cli/src/profile.rs, .env.example</files>
  <read_first>
    - crates/nameless-core/src/job.rs (the JobQueue trait + JobEnvelope this impl must satisfy)
    - crates/nameless-core/src/ports.rs (ObjectStore trait)
    - .planning/research/STACK.md §5 (sqlxmq reuses the sqlx pool; at-least-once, durability, retries, backpressure) + S3/R2 by-ID
    - .planning/phases/01-typed-capture-spine/01-CONTEXT.md (sqlxmq prod impl; --local vs server config via DATABASE_URL + NAMELESS_STORAGE_*)
  </read_first>
  <action>
    `queue_sqlxmq.rs` (`#[cfg(feature = "postgres")]`): `SqlxmqJobQueue` implementing `JobQueue` on sqlxmq over the shared `PgPool`. enqueue serializes `JobEnvelope` to JSON into the sqlxmq job table (durable — survives restart); retries use sqlxmq's attempt/backoff (align with `RetryPolicy`: max_attempts ~5, exponential); backpressure via a bounded channel / concurrency limit. Phase 1 still ENQUEUES only — do NOT register a running consumer here (Phase 2 adds the feature-worker consumer). Add an `#[ignore]` integration test that enqueues a job, drops + reopens the pool, and asserts the job is still present (durability-across-restart) — runs only against live Postgres.

    For sqlxmq add it to the `postgres` feature deps in `nameless-adapters/Cargo.toml`.

    `object_store_s3.rs` (gate behind the feature, e.g. `s3`/`postgres`): `S3ObjectStore` implementing `ObjectStore` against an S3-compatible endpoint (Cloudflare R2). Put writes bytes under the content-hash key with if-none-match / no-overwrite semantics (immutable); get/exists by key. Use the chosen client (`aws-sdk-s3`, R2-compatible via custom endpoint, OR the lighter `rust-s3` — pick one, document why; both were supply-chain-approved in Plan 01). Config from `NAMELESS_STORAGE_*` env. Mark live-bucket tests `#[ignore]`.

    `nameless-cli/src/profile.rs`: add the non-`--local` (server) profile that, behind the feature, builds `PostgresFragmentRepo` + `SqlxmqJobQueue` + `S3ObjectStore` from `DATABASE_URL` + `NAMELESS_STORAGE_*`. The default-feature CLI keeps ONLY the `--local` profile (so the lean binary still builds without the server deps). Add `.env.example` documenting every env var.
  </action>
  <verify>
    <automated>cargo build -p nameless-cli</automated>
    <env_gate>RAM-SAFE check (toolchain-gated): default `cargo build -p nameless-cli` builds the lean `--local`-only binary with NO server deps. ENV-GATED (user's real env): `cargo build --features postgres`; `DATABASE_URL=... cargo test -p nameless-adapters --features postgres -- --ignored` (sqlxmq durability-across-restart); `NAMELESS_STORAGE_*=... cargo test -p nameless-adapters --features postgres -- --ignored s3` (R2 put/get by content hash). These need a live Postgres + R2 bucket and a machine that can compile the heavy tree without OOM.</env_gate>
  </verify>
  <acceptance_criteria>
    - Default `cargo build -p nameless-cli` succeeds without tokio/sqlx/sqlxmq/S3 client in the tree.
    - `SqlxmqJobQueue` implements `JobQueue`; `S3ObjectStore` implements `ObjectStore` (compile under `--features postgres`).
    - `.env.example` lists DATABASE_URL + all NAMELESS_STORAGE_* vars.
    - (Env-gated) durability test: a job enqueued via sqlxmq is still present after the pool is dropped and reopened.
    - (Env-gated) S3 test: putting bytes then getting by content-hash key returns identical bytes; re-put of the same key does not mutate.
  </acceptance_criteria>
  <done>The durable sqlxmq queue + S3/R2 object store + server profile exist behind the feature; the default lean build is intact.</done>
</task>

</tasks>

<artifacts_produced>
- **Files:** `migrations/0001_init.sql`, `crates/nameless-adapters/src/repo_pg.rs`, `queue_sqlxmq.rs`, `object_store_s3.rs`, `.env.example` (modifies adapters `Cargo.toml` + `lib.rs`, cli `Cargo.toml` + `profile.rs`).
- **Feature:** `postgres` (optional tokio + sqlx 0.8.6 + sqlxmq [+ S3 client]).
- **Adapters:** `PostgresFragmentRepo`, `SqlxmqJobQueue`, `S3ObjectStore` (all implement the Plan 01/03 ports).
- **Schema:** `projects`, `fragments` tables; `provenance` + `fragment_state` Postgres enum types; pgvector extension enabled (no embedding columns yet — Phase 2).
- **CLI:** server profile (DATABASE_URL + NAMELESS_STORAGE_*) gated behind the feature; `--local` remains the default lean path.
- **Consumed by:** Phase 2 (feature/embedding columns + the FeatureExtract consumer on sqlxmq), Phases 7/8 (reference/stem/sample tables extend this schema).
</artifacts_produced>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI → Postgres | DATABASE_URL credentials + SQL across the network |
| CLI → S3/R2 | Storage credentials + audio bytes across the network |
| env → process | Secrets injected via environment variables |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | DB / storage credentials | mitigate | Secrets only via env (`DATABASE_URL`, `NAMELESS_STORAGE_*`); `.env.example` holds placeholders only; never logged; `.env` git-ignored. |
| T-04-02 | Tampering | SQL injection | mitigate | sqlx `query!`/`query_as!` parameterized + compile-time-checked; no string-built SQL. |
| T-04-03 | Tampering | object overwrite in S3/R2 | mitigate | Content-hash key + no-overwrite (if-none-match) put → immutable, matching the filesystem fake's contract. |
| T-04-04 | Denial of Service | queue flooding | mitigate | sqlxmq concurrency/backpressure + `RetryPolicy` max_attempts dead-letter; queue is durable + bounded. |
| T-04-SC | Tampering | tokio/sqlx/sqlxmq/S3 crate installs | mitigate | Supply-chain verified in Plan 01 Task 0 (full-phase crates.io checkpoint). |
</threat_model>

<verification>
**RAM-safe (toolchain-gated — the ONLY dev-machine-checkable items):**
- Default `cargo build -p nameless-adapters -p nameless-cli` succeeds with NO tokio/sqlx/sqlxmq/S3 client in the dependency tree (proves the `postgres` feature gate keeps the 4GB-buildable default intact).

**Env-gated (user's REAL environment — not the 4GB dev box; each needs the heavy compile and/or live services):**
- `cargo build -p nameless-adapters --features postgres` (heavy tree; may OOM on 4GB).
- `DATABASE_URL=postgres://... cargo sqlx migrate run` (apply `migrations/0001_init.sql`).
- `DATABASE_URL=... cargo test -p nameless-adapters --features postgres -- --ignored` (PostgresFragmentRepo round-trip + sqlxmq durability-across-restart).
- `NAMELESS_STORAGE_*=... cargo test -p nameless-adapters --features postgres -- --ignored s3` (R2 put/get by content hash, immutability).
</verification>

<success_criteria>
- Production Postgres repo, durable sqlxmq queue, and S3/R2 object store exist behind the `postgres` feature; the default build remains lean and 4GB-buildable. CAP-02 (immutable S3/R2 by-ID) and CAP-07 (durable Postgres-backed queue) production paths are delivered, with verification honestly env-gated.
</success_criteria>

<output>
Create `.planning/phases/01-typed-capture-spine/01-04-SUMMARY.md` when done.
</output>
