---
phase: 11-artistic-unified-ui-artistic-first-dark-single-surface-app-r
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - web/src/scene/NightBloom.tsx
  - web/src/scene/nightBloom.ts
  - web/src/scene/nightBloom.test.ts
  - web/src/scene/webgl.ts
  - web/src/scene/webgl.test.ts
  - web/src/scene/SceneLayer.tsx
  - web/src/scene/SceneLayer.test.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "On a WebGL-capable browser the landing hero is the living Night Bloom particle nebula; on no-WebGL/load-failure/test environments it is the designed static fallback, never a blank or broken hero"
    - "The scene dims to ~20% intensity in home and stays as the living background; the canvas mounts once and never unmounts across the landing/home transition"
    - "Under prefers-reduced-motion the scene renders a single static frame with parallax off"
    - "The three library is NOT in the initial bundle; it loads as a separate lazy chunk"
  artifacts:
    - path: "web/src/scene/NightBloom.tsx"
      provides: "R3F Night Bloom scene: GPU-buffered points, curl-like drift, ink-to-rose-to-amber ramp, 72 BPM luminance pulse, depth fog, pointer parallax, pulse ripple"
      min_lines: 120
    - path: "web/src/scene/nightBloom.ts"
      provides: "Pure, seeded particle attribute + color ramp + count heuristic functions"
      exports: ["createParticleAttributes", "particleCountFor", "colorRamp"]
    - path: "web/src/scene/webgl.ts"
      provides: "WebGL capability probe"
      exports: ["isWebGLAvailable"]
  key_links:
    - from: "web/src/scene/SceneLayer.tsx"
      to: "web/src/scene/NightBloom.tsx"
      via: "React.lazy dynamic import gated on isWebGLAvailable, wrapped in an error boundary falling back to the CSS backdrop"
      pattern: "lazy\\(.*import\\("
    - from: "web/src/scene/NightBloom.tsx"
      to: "web/src/scene/SceneDriver.ts"
      via: "useSceneDriver() drives the pulse ripple uniform"
      pattern: "useSceneDriver"
---

<objective>
Build the signature moment: the "Night Bloom" Three.js generative centerpiece per the UI-SPEC Three.js Scene Contract, lazy-loaded behind the SceneLayer seam so the home surface stays fast and every non-WebGL path lands on the designed fallback.

Decision traceability: D-04 (Three.js generative landing centerpiece, the hero is a living scene), D-03 (the scene IS the Sonder atmosphere: slow, spacious, analog-warm, 72 BPM breath), D-01 (glow lives ONLY inside the WebGL scene), D-09 (every row of the UI-SPEC scene contract table is binding). The SceneDriver audio-reactive port (D-04's M2 headroom) was created in plan 11-01; this plan makes the scene consume it.

Purpose: the landing is the phase's artistic thesis; it must be real craft, lazy, resilient, and accessible.
Output: NightBloom scene + pure attribute functions with tests, WebGL probe, SceneLayer completed with lazy load + error boundary + reduced-motion single-frame mode; bundle split verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-UI-SPEC.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-01-SUMMARY.md
@web/src/scene/SceneDriver.ts
@web/src/scene/SceneLayer.tsx
@web/src/styles/tokens.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure scene math + the Night Bloom R3F scene</name>
  <files>web/src/scene/nightBloom.ts, web/src/scene/nightBloom.test.ts, web/src/scene/NightBloom.tsx</files>
  <behavior>
    - Test: createParticleAttributes(8000, seed) returns Float32Array positions (length 8000*3) forming a loose disc (radial distribution bounded, thickness bounded), plus per-point seed and colorT arrays; same seed yields byte-identical output (deterministic), different seeds differ
    - Test: particleCountFor({cores: 8, deviceMemory: 8}) === 8000; particleCountFor({cores: 2}) === 4000; particleCountFor({deviceMemory: 2}) === 4000; missing fields treated as capable
    - Test: colorRamp(0) is the ink tone, colorRamp(0.5) nears dusty rose #D8A79F, colorRamp(1) nears muted amber #C9B277; all components in [0,1]
  </behavior>
  <action>
    1. `nightBloom.ts` (pure, no three imports, unit-testable in jsdom): a small seeded PRNG (mulberry32-style) feeding `createParticleAttributes(count, seed)` (disc placement with soft vertical thickness and density falloff toward the rim), `particleCountFor(caps)` (halve 8000 to 4000 below 4 cores or deviceMemory < 4, per the scene contract performance row), and `colorRamp(t)` interpolating the ink to dusty-rose to muted-amber ramp from the UI-SPEC.
    2. `NightBloom.tsx` (the only file allowed to import three / @react-three/fiber besides SceneLayer's dynamic import): an R3F `<Canvas>` with:
       - `<points>` + BufferGeometry built from createParticleAttributes; a custom ShaderMaterial: vertex shader displaces each point along smooth pseudo-noise flow (curl-like drift from layered sin/cos of position*frequencies + time; slow, breathing, never jittery), point size attenuated by depth; fragment shader draws a soft radial-falloff sprite (this is the ONLY glow in the app, additive blending), color from the ramp via each point's colorT, faded by depth fog into #0B0A10 so canvas and page read as one surface.
       - Uniforms: uTime; uPulseTravel driving a gentle luminance wave through the field at 1.2 Hz (72 BPM, the tempo-without-sound row); uIntensity (prop, 1.0 landing / 0.2 home, eased over the 900ms transition); uBloom for the capture ripple: `useSceneDriver()` pulse subscription kicks uBloom to 1 and it decays over <=1s (SceneDriver.pulse acknowledgment, wired from CaptureSheet in plan 11-02).
       - Pointer parallax: a ref updated from pointermove on the window; camera target eased toward it each frame with critically damped smoothing inside useFrame; NEVER setState per frame, and no scroll listeners of any kind (motion contract ban).
       - Performance: `dpr={[1, 1.75]}`; on `document.visibilitychange` hidden, stop the frameloop (set to 'never') and resume on visible; dispose geometry and material in the cleanup effect; particle count from particleCountFor(navigator capabilities).
       - Reduced motion (matchMedia `prefers-reduced-motion: reduce`): render exactly one frame (frameloop 'never' after a single manual advance/invalidate), parallax disabled, pulse becomes a 150ms opacity blink on the canvas element instead of the ripple uniform.
       - `<canvas aria-hidden>` via Canvas props; the scene carries zero information not in the DOM (accessibility row).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/scene</automated>
  </verify>
  <done>Pure functions tested (deterministic attributes, count heuristic, ramp endpoints); NightBloom compiles with the full contract implemented: drift, ramp, 1.2 Hz pulse, fog, parallax, DPR clamp, visibility pause, dispose, reduced-motion single frame, pulse ripple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SceneLayer lazy integration, WebGL probe, fallback guarantee, bundle split</name>
  <files>web/src/scene/webgl.ts, web/src/scene/webgl.test.ts, web/src/scene/SceneLayer.tsx, web/src/scene/SceneLayer.test.tsx</files>
  <behavior>
    - Test: isWebGLAvailable() returns false in jsdom (getContext yields null) and true when a stubbed canvas returns a context object
    - Test: SceneLayer in jsdom renders the .scene-fallback backdrop and never the canvas (the designed fallback IS the tested path, per the UI-SPEC and course-law)
    - Test: SceneLayer keeps aria-hidden on the whole layer and applies the intensity treatment for both 1.0 and 0.2
    - Test: if the lazy chunk import rejects, the error boundary renders the fallback (simulate with a failing lazy factory)
  </behavior>
  <action>
    1. `webgl.ts`: `isWebGLAvailable()` probing an offscreen canvas for 'webgl2' then 'webgl', try/catch, memoized.
    2. Complete `SceneLayer.tsx`: keep the CSS fallback rendering unconditionally as the base layer; when `isWebGLAvailable()` and not in a test env, mount `React.lazy(() => import('./NightBloom'))` inside `<Suspense fallback={null}>` and a small error boundary component; the fallback stays visible until the scene's first frame so landing never flashes blank (fallback row: never a blank or broken hero). Pass `intensity` and the driver through. The canvas element persists across landing/home (mounted once in App from plan 11-01; verify no remount on state change by keying nothing on app state).
    3. Bundle discipline (phase constraint: three is heavy): the ONLY reference to NightBloom outside scene/ is this dynamic import, so Vite splits it into its own chunk. Run `npm run build` and confirm the entry chunk stays lean and a separate heavy scene chunk exists (gate below).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build && node -e "const fs=require('fs');const d='dist/assets';const js=fs.readdirSync(d).filter(f=>f.endsWith('.js'));const entry=js.filter(f=>/^index-/.test(f));const sizes=Object.fromEntries(js.map(f=>[f,fs.statSync(d+'/'+f).size]));console.log(sizes);const entrySize=entry.reduce((a,f)=>a+sizes[f],0);const others=js.filter(f=>!/^index-/.test(f));if(js.length<2||entrySize>400000||!others.some(f=>sizes[f]>300000)){console.error('scene chunk not split or entry too heavy');process.exit(1)}"</automated>
  </verify>
  <done>jsdom/test env exercises the fallback path end-to-end; WebGL probe + error boundary guarantee a designed hero everywhere; build produces a separate heavy lazy chunk with a lean entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPU/driver → app | WebGL context creation can fail or be blocked |
| pointer events → scene | only numeric coordinates cross; no user strings enter shaders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Denial of Service | RAF loop + particle count | mitigate | DPR clamp 1.75, count heuristic halving, frameloop paused on hidden tab, dispose on unmount |
| T-11-07 | Tampering | shader uniforms | accept | all uniforms are app-computed numbers; no user-controlled data reaches GLSL |
| T-11-SC | Tampering | npm installs | mitigate | no new installs; three/@react-three/fiber vetted at the 11-01 legitimacy checkpoint |
</threat_model>

<verification>
- `cd web && npx vitest run` green (pure scene math + fallback/probe/boundary tests)
- `cd web && npm run build` green with verified chunk split (entry lean, scene chunk separate)
- Visual confirmation of the living scene is env-gated to a WebGL browser and lands in the plan 11-06 end-of-phase human check
</verification>

<success_criteria>
- Night Bloom implements every row of the UI-SPEC scene contract table (D-04, D-03)
- Fallback is designed, tested, and automatic on no-WebGL/failure/reduced-capability paths
- three never loads unless the browser can use it; home surface stays fast
</success_criteria>

<output>
Create `.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-03-SUMMARY.md` when done
</output>
