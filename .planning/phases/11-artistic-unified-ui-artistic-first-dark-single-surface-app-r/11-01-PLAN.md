---
phase: 11-artistic-unified-ui-artistic-first-dark-single-surface-app-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/package-lock.json
  - web/index.html
  - web/src/styles/tokens.css
  - web/src/styles/base.css
  - web/src/copy.ts
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/App.test.tsx
  - web/src/shell/Landing.tsx
  - web/src/shell/HomeSurface.tsx
  - web/src/shell/Dock.tsx
  - web/src/shell/ProjectSwitcher.tsx
  - web/src/shell/StartProject.tsx
  - web/src/shell/shell.test.tsx
  - web/src/scene/SceneDriver.ts
  - web/src/scene/SceneDriver.test.ts
  - web/src/scene/SceneLayer.tsx
  - web/src/overlays/OverlayContext.tsx
  - web/src/overlays/OverlayRoot.tsx
  - web/src/whispers/WhisperContext.tsx
  - web/src/test/renderWithApi.tsx
  - web/src/styles.css (deleted)
  - web/src/screens/ (deleted)
  - web/src/components/ (deleted)
  - web/src/hooks/useFragments.ts (deleted)
  - web/src/hooks/useProject.ts (deleted)
  - web/src/hooks/useReferences.ts (deleted)
  - web/src/hooks/useStemLibrary.ts (deleted)
  - web/src/lib/graph.ts (deleted)
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Opening the app shows a landing composition: wordmark, tagline, one CTA over the designed dark backdrop, nothing else"
    - "Clicking 'Enter the studio' transitions to the single home surface; there are no routed pages anywhere"
    - "With zero projects, home shows the Start-a-project moment and no dock; creating a project reveals the dock"
    - "The dock shows exactly 4 labeled actions plus the project switcher"
    - "npx vitest run, npx tsc --noEmit, and npm run build all pass"
  artifacts:
    - path: "web/src/styles/tokens.css"
      provides: "Every design token from the UI-SPEC: color, spacing, type, z-index, radius, motion"
      contains: "--z-scene"
    - path: "web/src/copy.ts"
      provides: "The full binding copy deck from the UI-SPEC Copywriting Contract, typed"
      min_lines: 60
    - path: "web/src/App.tsx"
      provides: "Two-state shell: landing | home, no react-router"
    - path: "web/src/scene/SceneDriver.ts"
      provides: "SceneDriver DI port with idle driver + test fake"
      exports: ["SceneDriver", "createIdleSceneDriver", "FakeSceneDriver"]
    - path: "web/src/shell/Dock.tsx"
      provides: "4-action dock dispatching overlay intents"
  key_links:
    - from: "web/src/main.tsx"
      to: "web/src/api/createClient.ts"
      via: "createClient() injected through the existing ApiProvider, unchanged"
      pattern: "createClient\\(\\)"
    - from: "web/src/shell/Dock.tsx"
      to: "web/src/overlays/OverlayContext.tsx"
      via: "dock actions call openOverlay(kind)"
      pattern: "openOverlay"
    - from: "web/src/App.tsx"
      to: "web/src/scene/SceneLayer.tsx"
      via: "SceneLayer mounted once at z-scene with intensity prop driven by app state"
      pattern: "SceneLayer"
---

<objective>
Lay the foundation of the Artistic Unified UI: install the five approved dependencies, build the complete design-token system and binding copy deck from the UI-SPEC, and replace the routed multi-view app with the two-state shell (landing | home) including the dock, project switcher, Start-a-project moment, and the DI seams (SceneDriver port, SceneLayer mount, OverlayContext) that waves 2 and 3 fill in.

Decision traceability: implements D-01 (artistic-first, bespoke tokens over any component library), D-02 (dark palette per UI-SPEC Color section), D-05 (ONE home surface, router removed, per 11-CONTEXT.md), D-06 (progressive disclosure layer model, z-tokens), D-08 (first-run path: Start-a-project before dock), D-09 (UI-SPEC is the binding contract). D-04's WebGL scene lands in plan 11-03 over the SceneDriver/SceneLayer seam created here.

Purpose: every later plan builds on these tokens, this copy deck, and these seams; getting the shell and file ownership right here is what lets waves 2 and 3 run in parallel without conflicts.
Output: a green (vitest + tsc + build) two-state app shell with the designed fallback backdrop, dock, switcher, and Start-a-project moment; old screens/components deleted; api/ seam untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-UI-SPEC.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-CONTEXT.md
@.planning/ENGINEERING-PRINCIPLES.md
@web/src/api/NamelessApi.ts
@web/src/api/createClient.ts
@web/src/api/context.tsx
@web/src/ActiveProjectContext.tsx
@web/src/main.tsx
@web/src/App.tsx
@web/src/test/renderWithApi.tsx
@web/vite.config.ts
@web/package.json
</context>

<interfaces>
Seams this plan creates for later waves (exact shapes, so waves 2/3 need no renegotiation):

```ts
// web/src/scene/SceneDriver.ts (UI-SPEC "Three.js Scene Contract", audio-reactive port row)
export interface SceneDriver {
  amplitude(): number;          // 0..1; idle driver returns 0
  pulse(): void;                // one visible bloom ripple; idle driver is a no-op that records nothing
}
export function createIdleSceneDriver(): SceneDriver;
export class FakeSceneDriver implements SceneDriver { pulses: number; /* test fake, counts pulse() calls */ }
// React context: SceneDriverProvider / useSceneDriver(), default = idle driver

// web/src/overlays/OverlayContext.tsx (UI-SPEC "Surface Architecture", layer 3)
export type OverlayIntent =
  | { kind: 'capture' }
  | { kind: 'fragment'; fragmentId: string }
  | { kind: 'references' }
  | { kind: 'stems' }
  | { kind: 'credits' };
export function useOverlay(): { open: OverlayIntent | null; openOverlay(i: OverlayIntent): void; closeOverlay(): void };

// web/src/scene/SceneLayer.tsx
// Props: { intensity: number; driver?: SceneDriver }. In this plan it renders ONLY the designed
// CSS fallback backdrop (UI-SPEC fallback row). Plan 11-03 adds the lazy WebGL path inside scene/.

// web/src/copy.ts — a single exported const `copy` object keyed by surface
// (landing, startProject, capture, whispers, references, stems, attribution, credits, errors, rights).
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for the five new npm dependencies</name>
  <files>none (no files change in this task; it blocks Task 2's installs)</files>
  <action>Pause BEFORE any npm install runs. No RESEARCH.md Package Legitimacy Audit exists for this phase, so per the fallback policy all five packages the approved UI-SPEC allows are treated as [ASSUMED]: present the verification list below to the human and wait. This checkpoint is never auto-approvable; workflow.auto_advance does not apply.</action>
  <what-built>Nothing yet. No RESEARCH.md Package Legitimacy Audit exists for this phase, so per the fallback policy all five packages the approved UI-SPEC allows are treated as [ASSUMED] and require human verification BEFORE any install runs.</what-built>
  <how-to-verify>
    Verify each package is the canonical, legitimate publication by opening its npm page and checking publisher + weekly downloads (all five are top-tier, widely used packages; anything that looks like a low-download namesquat fails):
    1. https://www.npmjs.com/package/three (three.js core, ~millions weekly)
    2. https://www.npmjs.com/package/@react-three/fiber (pmndrs; install the v8 line for React 18)
    3. https://www.npmjs.com/package/@phosphor-icons/react (phosphor-icons org)
    4. https://www.npmjs.com/package/@fontsource-variable/hanken-grotesk (fontsource org)
    5. https://www.npmjs.com/package/@fontsource/jetbrains-mono (fontsource org)
    Dev-only addition (types, no runtime code): https://www.npmjs.com/package/@types/three (DefinitelyTyped).
  </how-to-verify>
  <resume-signal>Type "approved" to allow the installs in Task 2, or name any package that failed verification</resume-signal>
  <verify>Human has confirmed each of the six npm pages shows the canonical publisher and healthy download counts</verify>
  <done>Human replied "approved" (or flagged packages were replaced/removed before any install ran)</done>
</task>

<task type="auto">
  <name>Task 2: Install deps, author tokens.css + base.css + the binding copy deck, delete the replaced views</name>
  <files>web/package.json, web/package-lock.json, web/index.html, web/src/styles/tokens.css, web/src/styles/base.css, web/src/copy.ts, web/src/styles.css (delete), web/src/screens/ (delete), web/src/components/ (delete), web/src/hooks/useFragments.ts (delete), web/src/hooks/useProject.ts (delete), web/src/hooks/useReferences.ts (delete), web/src/hooks/useStemLibrary.ts (delete), web/src/lib/graph.ts (delete)</files>
  <action>
    1. Install (per UI-SPEC "Allowed new deps", D-09): `npm i three @react-three/fiber@^8 @phosphor-icons/react @fontsource-variable/hanken-grotesk @fontsource/jetbrains-mono` and `npm i -D @types/three`. @react-three/fiber MUST stay on the v8 line (v9 requires React 19; this app is React 18). Remove `react-router-dom` from dependencies (D-05: single surface needs no routes; UI-SPEC allows removal).
    2. Create `web/src/styles/tokens.css` transcribing EVERY token from the UI-SPEC verbatim as CSS custom properties on `:root`: the full Color table (dominant `#0B0A10`, secondary panel/card/border values, accent `#D8A79F`, destructive `#E08D8D`, `--text`/`--text-muted`/`--text-dim`/`--amber`/`--ok`/`--focus`, input bg `#0F0E15`), the Spacing Scale (xs 4 through 3xl 64, plus the 44px hit-area and 72px record-button exceptions as named tokens), Typography sizes/weights/line-heights (body 15/400/1.55, label 13/400/1.4, heading 20/600/1.25, display clamp(40px, 6vw, 76px)/600/1.05), the five z-index tokens (`--z-scene:0`, `--z-surface:10`, `--z-dock:20`, `--z-overlay:30`, `--z-toast:40`; these five ONLY), radius lock (panels 12px, inputs 8px, pills 9999px; nothing above 16px), motion tokens (ease `cubic-bezier(0.16, 1, 0.3, 1)`, durations 150/200/260/420/900ms), and shadow recipe (ink-tinted `rgba(5,4,10,...)`, blur max 24px, overlays only). Add a `prefers-reduced-motion: reduce` block that collapses the duration tokens to 150ms/0.
    3. Create `web/src/styles/base.css`: font imports are done in main.tsx (fontsource packages), so base.css sets the global font stacks (Hanken Grotesk with the UI-SPEC fallback stack; `--font-mono` with JetBrains Mono + ui-monospace fallbacks), body background `var(--ink)`, text color `var(--text)`, focus-visible ring `2px solid var(--focus)` with 2px offset globally and never removed, the skip-link style, selection color, and the designed static fallback backdrop class `.scene-fallback` (radial gradient `#181826` bloom at 70% -10% over ink, matching the old styles.css DNA, plus a fine noise grain on a `pointer-events:none` fixed pseudo-element, per the UI-SPEC fallback row). Delete `web/src/styles.css`.
    4. Create `web/src/copy.ts`: transcribe the UI-SPEC "Copywriting Contract" full copy deck VERBATIM into one typed exported const (nested by surface: landing, startProject, capture, whispers, references, stems, attribution, credits, errors, notFound, rights). Include the four rights option labels ("Copyrighted, uncleared" / "Royalty-free" / "My own work" / "Unknown"), the persistent rights note, the 422 banner strings, and the generic error copy. HARD RULE for this file: no em dash (U+2014) or en dash (U+2013) characters anywhere in the file, including comments; where the spec's tempo-range display needs a separator, later components use the word "to" or a middle dot. All later plans import from this file and treat it read-only.
    5. Delete the replaced views and their tests: `web/src/screens/` (all), `web/src/components/` (all), `web/src/hooks/useFragments.ts`, `useProject.ts`, `useReferences.ts`, `useStemLibrary.ts`, `web/src/lib/graph.ts`, and `web/src/App.test.tsx` (a new one lands in Task 3). KEEP untouched: everything in `web/src/api/`, `web/src/lib/` (except graph.ts), `web/src/hooks/useAsyncData.ts` + its test, `ActiveProjectContext.tsx`, `web/src/test/setup.ts`. The api/ adapter tests must survive this phase.
    6. Update `web/index.html`: title "Nameless", `<meta name="theme-color" content="#0B0A10">`, dark background on the root element so first paint is ink, `color-scheme: dark`.
  </action>
  <verify>
    <automated>cd web && node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; const need=['three','@react-three/fiber','@phosphor-icons/react','@fontsource-variable/hanken-grotesk','@fontsource/jetbrains-mono','@types/three']; const bad=need.filter(n=>!d[n]); if(bad.length||p.dependencies['react-router-dom']){console.error('missing:',bad,'router:',!!p.dependencies['react-router-dom']);process.exit(1)}" && node -e "const fs=require('fs'); const c=fs.readFileSync('src/copy.ts','utf8'); if(/[–—]/.test(c)){console.error('dash chars found in copy.ts');process.exit(1)}"</automated>
  </verify>
  <done>Five deps + @types/three installed, react-router-dom gone from package.json; tokens.css carries every UI-SPEC token; copy.ts carries the full deck with zero dash characters; old screens/components/screen-hooks deleted; api/ and lib/ survivors intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two-state shell: landing, home surface, dock, project switcher, Start-a-project, and the wave-2 seams</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/App.test.tsx, web/src/shell/Landing.tsx, web/src/shell/HomeSurface.tsx, web/src/shell/Dock.tsx, web/src/shell/ProjectSwitcher.tsx, web/src/shell/StartProject.tsx, web/src/shell/shell.test.tsx, web/src/scene/SceneDriver.ts, web/src/scene/SceneDriver.test.ts, web/src/scene/SceneLayer.tsx, web/src/overlays/OverlayContext.tsx, web/src/overlays/OverlayRoot.tsx, web/src/whispers/WhisperContext.tsx, web/src/test/renderWithApi.tsx</files>
  <behavior>
    - Test: renders landing with wordmark "Nameless", the tagline from copy.ts, and one CTA; max 3 text elements; no dock present
    - Test: clicking the CTA switches to home (landing copy gone, `main` landmark present); no route/URL change involved
    - Test: with a mock api returning zero projects, home shows the Start-a-project heading from copy.ts and NO dock (D-08 first-run path)
    - Test: creating a project via the Start-a-project input makes the dock appear with exactly 4 action buttons (Capture, References, Stems, Credits) and the switcher
    - Test: dock's Capture button carries the accent styling class and calls openOverlay({kind:'capture'}) (assert via OverlayContext state)
    - Test: FakeSceneDriver counts pulse() calls; createIdleSceneDriver().amplitude() === 0
    - Test: reduced-motion query mocked true renders the app without the enter transition classes
  </behavior>
  <action>
    1. `web/src/scene/SceneDriver.ts`: implement the SceneDriver port exactly as in `<interfaces>` (D-04 seam; testability law: port + idle impl + FakeSceneDriver test fake). Export a `SceneDriverProvider`/`useSceneDriver` React context defaulting to the idle driver.
    2. `web/src/scene/SceneLayer.tsx`: a `position:fixed; inset:0; z-index:var(--z-scene)` layer, `aria-hidden="true"`, rendering the `.scene-fallback` backdrop from base.css. Accept `{ intensity: number }` and expose it as a CSS opacity/dim treatment so landing (1.0) reads brighter than home (0.2) even in fallback mode. Structure the component so plan 11-03 can add a lazy WebGL path inside `web/src/scene/` without touching any file outside that directory.
    3. `web/src/overlays/OverlayContext.tsx`: the OverlayIntent union + provider + `useOverlay()` exactly as in `<interfaces>`. One overlay at a time is a type-level fact (a single `open` slot, D-06). `web/src/overlays/OverlayRoot.tsx`: renders per `open.kind`; in this plan every branch renders null (plan 11-02 replaces this file with the real primitives + registry; only plan 11-02 touches it next).
    4. `web/src/whispers/WhisperContext.tsx`: context with `whisper(text: string, opts?: {error?: boolean})` and internal queue state; the visible region component lands in plan 11-02, so this plan ships the provider + hook + a no-op default so callers compile and tests can assert queued whispers.
    5. `web/src/App.tsx`: the two-state shell. `useState<'landing'|'home'>('landing')`. Landing state renders `Landing` (left-aligned composition per UI-SPEC Surface Architecture: Display wordmark "Nameless", tagline and CTA from copy.ts, nothing else, over SceneLayer at intensity 1.0). CTA click starts the enter transition (900ms scene dim to 0.2, 300ms copy crossfade out, home fade-in 500ms delayed 200ms, all via the motion tokens and transform/opacity only; under reduced motion: instant cut with 150ms crossfade) then state becomes 'home'. Home state renders: SceneLayer (intensity 0.2), `HomeSurface` at `--z-surface` (project name top-left; when zero projects: `StartProject`; otherwise an empty-sky region that plan 11-04 replaces with the constellation, showing the constellation empty-state copy from copy.ts centered), `Dock` at `--z-dock` (only when a project exists), `OverlayRoot` at `--z-overlay`. Keep the skip link; landmarks per the Accessibility Contract: `header` (switcher), `main` (surface), `nav` (dock), and keep `createError` surfacing from the old App when project creation fails.
    6. `web/src/shell/Dock.tsx`: single bottom row, max 72px tall, 4 labeled icon actions using @phosphor-icons/react regular weight (Capture = accent primary and the ONLY accent element in the dock per the Color reserved list; References; Stems; Credits) + the ProjectSwitcher. Every icon-only control has a 44px minimum hit area; every action has a visible text label (jargon-free, D-08). Actions call `openOverlay` with their intent.
    7. `web/src/shell/ProjectSwitcher.tsx`: quiet text button (top-left header) opening a small popover listing projects + a "New project" inline input using `useProjects`-style logic against the injected api (re-implement the data hook inline or colocated; the old useProjects was deleted). `web/src/shell/StartProject.tsx`: the Start-a-project moment from the copy deck (single input + CTA + body line), inline on the surface, using `createProject` then `setActiveProjectId`.
    8. `web/src/main.tsx`: composition root without BrowserRouter: import the two fontsource packages, tokens.css, base.css; wrap `ApiProvider(createClient())` > `ActiveProjectProvider` > `SceneDriverProvider` > `WhisperProvider` > `OverlayProvider` > `App`. The api seam stays byte-identical in behavior (D-07 rests on it).
    9. Update `web/src/test/renderWithApi.tsx`: drop MemoryRouter; add the new providers (scene driver accepts an injected FakeSceneDriver via options). Keep the api/activeProjectId options as-is so existing api-level tests keep passing.
    10. Styling throughout uses component-scoped CSS files importing the tokens (no inline hex values; every color/space/z/radius/duration comes from tokens.css). Anti-slop guardrails apply (D-01, D-09): no card grids, no uppercase tracked eyebrows, no glow on DOM, radius lock.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>App boots to landing (wordmark/tagline/CTA over the designed fallback), enters home on CTA, shows Start-a-project when empty and the 4-action dock + switcher once a project exists; no react-router anywhere; SceneDriver/OverlayContext/Whisper seams exist with tests; vitest + tsc + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | new third-party packages execute in the build and ship to the browser |
| API responses → DOM | project titles and notes are user-entered strings rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-SC | Tampering | npm installs (three, @react-three/fiber, @phosphor-icons/react, fontsource x2, @types/three) | mitigate | Task 1 blocking human legitimacy checkpoint before install; exact packages pinned in package-lock; no postinstall scripts expected from any of these |
| T-11-01 | Tampering/XSS | rendering API strings (titles, notes) | mitigate | render untrusted strings as React text nodes only; no raw-HTML injection props anywhere (gated in plan 11-06) |
| T-11-02 | Information Disclosure | fonts/icons loaded from network | mitigate | fonts self-hosted via fontsource npm packages, zero network font requests (local-first, per UI-SPEC) |
</threat_model>

<verification>
- `cd web && npx vitest run` green (api adapter tests + lib tests + useAsyncData survive; new shell tests pass)
- `cd web && npx tsc --noEmit` green
- `cd web && npm run build` green
- No `react-router-dom` in package.json dependencies
</verification>

<success_criteria>
- Two-state shell live: landing → home, no routes (D-05)
- Full UI-SPEC token system + copy deck exist as the single sources of truth (D-01, D-02, D-09)
- Seams (SceneDriver, SceneLayer, OverlayContext, WhisperContext) ready for waves 2-3
- First-run path stages correctly: Start-a-project before dock (D-08)
- All old view code deleted; api/ seam untouched; suite green
</success_criteria>

<output>
Create `.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-01-SUMMARY.md` when done
</output>
