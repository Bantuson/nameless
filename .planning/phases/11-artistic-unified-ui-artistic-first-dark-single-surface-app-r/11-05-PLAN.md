---
phase: 11-artistic-unified-ui-artistic-first-dark-single-surface-app-r
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - web/src/lib/clock.ts
  - web/src/lib/clock.test.ts
  - web/src/lib/humanize.ts
  - web/src/lib/humanize.test.ts
  - web/src/overlays/ReferenceDrawer.tsx
  - web/src/overlays/ReferenceDrawer.test.tsx
  - web/src/overlays/TonalBalanceBars.tsx
  - web/src/overlays/StemDrawer.tsx
  - web/src/overlays/StemDrawer.test.tsx
  - web/src/overlays/AttributionForm.tsx
  - web/src/overlays/AttributionForm.test.tsx
autonomous: true
requirements: [UI-02, UI-03]

must_haves:
  truths:
    - "User can upload a reference, watch 'Learning the atmosphere...' resolve into a summary whose vibe prose comes FIRST and measurables follow in mono, and attach it to the project as Vibe or Sonic target"
    - "The non-cloning note is always visible in the reference drawer"
    - "User can separate a track into stems, browse stem rows, and advance in-drawer to the attribution form via 'Sample this stem'"
    - "Submitting incomplete attribution renders the designed 422 state: coral banner stating nothing was added, per-field 'required for the credit' lines, focus moved to the first missing field"
    - "A complete attribution submits, whispers 'Sampled. {artist} is on the credits.', and nothing about the rights note pretends attribution is permission"
  artifacts:
    - path: "web/src/lib/clock.ts"
      provides: "m:ss.mmm parse/format for time-range inputs"
      exports: ["formatClock", "parseClock"]
    - path: "web/src/lib/humanize.ts"
      provides: "AttributionField to human label mapping from the UI-SPEC 422 contract"
      exports: ["humanizeAttributionField"]
    - path: "web/src/overlays/ReferenceDrawer.tsx"
      provides: "Reference list, upload, vibe-first summary card, tonal-balance bars, attach flow"
      min_lines: 100
    - path: "web/src/overlays/AttributionForm.tsx"
      provides: "Attribution form incl. the designed 422 incomplete_attribution state"
      min_lines: 80
  key_links:
    - from: "web/src/overlays/ReferenceDrawer.tsx"
      to: "web/src/api/NamelessApi.ts"
      via: "listReferences / uploadReference / getReferenceSummary (poll until analysis) / attachReference"
      pattern: "getReferenceSummary"
    - from: "web/src/overlays/StemDrawer.tsx"
      to: "web/src/api/NamelessApi.ts"
      via: "listReferences (tracks) / separateStems / listStems"
      pattern: "separateStems"
    - from: "web/src/overlays/AttributionForm.tsx"
      to: "web/src/lib/attribution.ts"
      via: "missingAttributionFields drives the disabled submit + client-side field naming"
      pattern: "missingAttributionFields"
    - from: "web/src/overlays/AttributionForm.tsx"
      to: "web/src/api/errors.ts"
      via: "catch IncompleteAttributionError and render the 422 designed state from error.missing"
      pattern: "IncompleteAttributionError"
---

<objective>
Complete the taste-and-provenance half of the surface: the Reference drawer (UI-02) and the Stem drawer with the attribution form and its designed 422 state (UI-03). This is where the phase's integrity story is felt: non-cloning stated in-context, attribution completeness enforced honestly on both sides of the wire.

Decision traceability: D-07 (summaries over raw data: vibe prose leads, measurables follow; the drawer explains what the system learned and refuses), D-06 (drawer depth advances in place with a back affordance, never a new page), D-08 (sample and credits are steps three and four of the tutorial-free core loop), D-09 (Component Inventory rows for Reference drawer, Stem drawer, Attribution form, and the 422 contract are binding).

Purpose: UI-02 and UI-03 delivered inside the single surface with every designed state.
Output: both drawers complete with all four component states, the attribution 422 experience, dash-free time-range inputs, and pure helpers with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-UI-SPEC.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-02-SUMMARY.md
@web/src/api/NamelessApi.ts
@web/src/api/types.ts
@web/src/api/errors.ts
@web/src/api/fixtures.ts
@web/src/overlays/primitives/DrawerShell.tsx
@web/src/whispers/WhisperContext.tsx
@web/src/copy.ts
@web/src/lib/attribution.ts
@web/src/lib/format.ts
@web/src/lib/rights.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure helpers: m:ss.mmm clock + attribution field humanization</name>
  <files>web/src/lib/clock.ts, web/src/lib/clock.test.ts, web/src/lib/humanize.ts, web/src/lib/humanize.test.ts</files>
  <behavior>
    - Test: formatClock(0) === "0:00.000"; formatClock(83450) === "1:23.450"; formatClock(3723004) handles minutes over 59 as total minutes ("62:03.004")
    - Test: parseClock round-trips formatClock for a spread of values; parseClock accepts "1:23.4" and "1:23" (missing millis padded); parseClock returns null for blank, negative, malformed, or out-of-range seconds (>=60)
    - Test: humanizeAttributionField covers all seven AttributionField values exactly per the UI-SPEC mapping (source track, stem, title, artist, stem type, time range, rights status) and is exhaustive at the type level
  </behavior>
  <action>
    1. `lib/clock.ts`: pure `formatClock(ms: number): string` and `parseClock(input: string): number | null` for the attribution form's start/end inputs (UI-SPEC Attribution form row: m:ss.mmm mono inputs). No Date objects; integer math only.
    2. `lib/humanize.ts`: `humanizeAttributionField(f: AttributionField): string` implementing the UI-SPEC 422 field-name humanization table verbatim, using an exhaustive switch so a new field variant fails tsc.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/lib/clock.test.ts src/lib/humanize.test.ts</automated>
  </verify>
  <done>Both helpers pure, exhaustively tested, matching the spec tables exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reference drawer with vibe-first summary and attach flow</name>
  <files>web/src/overlays/ReferenceDrawer.tsx, web/src/overlays/ReferenceDrawer.test.tsx, web/src/overlays/TonalBalanceBars.tsx</files>
  <behavior>
    - Test: empty library renders the copy-deck empty state ("No references yet" + body + "Upload a reference" CTA); loading renders skeleton rows, no spinners
    - Test: uploading a file calls uploadReference and the new item shows "Learning the atmosphere..." with a skeleton summary while analysis is null; with a scripted fake api + fake timers, getReferenceSummary is re-polled at 5s until analysis arrives
    - Test: an analyzed reference renders vibe prose FIRST (Body type), then the measurables grid in mono: genre, tempo range (dash-free "110 to 116 BPM"), LUFS, stereo width, and exactly 5 tonal-balance bars whose heights follow the tonal_balance array
    - Test: attach flow offers the two roles with the copy-deck labels ("Vibe: steer the mood" / "Sonic target: match the measurables"), confirms, calls attachReference with the chosen role, and whispers confirmation
    - Test: the non-cloning note from copy.ts is present in the drawer in every state
  </behavior>
  <action>
    1. Complete `ReferenceDrawer.tsx` (replacing the plan 11-02 shell) in DrawerShell: list view (title/artist or the copy-deck fallback for untitled, analyzed tick in --ok when ready) advancing in-place to a detail pane with a back affordance (DrawerShell depth support from plan 11-02). Upload control (file input + optional title/artist fields, labels above inputs) calling uploadReference then polling that reference's summary at 5s until `analysis != null` (Component Inventory row).
    2. Summary card per D-07 ordering: vibe prose leads as readable Body text; the measurables grid follows in JetBrains Mono (genre, tempoRange(min,max), lufs(), stereoWidth() from lib/format, all dash-free after plan 11-02's cleanup). `TonalBalanceBars.tsx`: 5 slim vertical bars, low to high, `--text-dim` fills, NO background tracks, band names from TONAL_BAND_NAMES as quiet labels; heights proportional to the tonal_balance ratios; the types cannot carry raw arrays beyond these 5 ratios and the UI must not fake more.
    3. Attach: role choice as two describable options with the copy-deck labels + a confirm button; on success whisper and reflect attached state. The persistent non-cloning line (copy.ts references note) renders in the drawer footer in muted text, always visible (11-CONTEXT.md non-cloning integrity + UI-SPEC).
    4. All four component states designed; errors use the in-overlay coral banner pattern, never a modal on a modal.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/overlays/ReferenceDrawer.test.tsx</automated>
  </verify>
  <done>UI-02 complete in-drawer: upload, poll, vibe-first summary with 5-bar tonal balance, attach with roles, non-cloning note ever-present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stem drawer + attribution form with the designed 422 state</name>
  <files>web/src/overlays/StemDrawer.tsx, web/src/overlays/StemDrawer.test.tsx, web/src/overlays/AttributionForm.tsx, web/src/overlays/AttributionForm.test.tsx</files>
  <behavior>
    - Test: track list renders from listReferences; a track with no stems shows "Separate into stems"; clicking it calls separateStems, shows "Splitting into stems..." and polls listStems at 5s until rows arrive (scripted fake + fake timers)
    - Test: a stem row shows the type badge, duration in mono, and the separator model as dim mono meta; its CTA "Sample this stem" advances in-drawer to the attribution form with a visible back affordance
    - Test: submit stays disabled while missingAttributionFields is non-empty and the disabled state names what is missing using humanized labels
    - Test: an api throwing IncompleteAttributionError(['source_artist','rights']) renders the 422 banner ("The credit is incomplete" / "Nothing was added. Complete the missing fields: artist, rights status."), turns those field labels coral with the per-field "required for the credit" line, and moves focus to the first missing field; the form stays open and no whisper fires
    - Test: a complete submission calls addSample with {stemId, projectId, artist, startMs, endMs, rights, title}, whispers "Sampled. {artist} is on the credits.", and returns to the stems pane
    - Test: time inputs accept m:ss.mmm text, parse via parseClock, reject end <= start via the shared predicate, and render errors below inputs with aria-describedby
  </behavior>
  <action>
    1. Complete `StemDrawer.tsx` (replacing the plan 11-02 shell) in DrawerShell: tracks pane (listReferences; a reference doubles as a track per the api seam) then per-track stems pane. Separate CTA and working copy from copy.ts; poll listStems at 5s after separateStems until stems appear, clearing on unmount. Stem row per Component Inventory; empty state from copy.ts ("No stems yet" + body).
    2. `AttributionForm.tsx` as the drawer's third depth pane (title "Sample this stem", submit "Add sample"): fields with labels ABOVE inputs, errors below inputs referenced via aria-describedby (Accessibility Contract): artist (required), title (optional, helper "falls back to the track title"; PREFILL with the source track's title so the client-side predicate matches the server's fallback semantics), start/end as m:ss.mmm mono text inputs via formatClock/parseClock, rights select using the copy-deck labels ("Copyrighted, uncleared" / "Royalty-free" / "My own work" / "Unknown") mapped to RightsStatus values. The persistent quiet rights note from copy.ts renders under the form ("Attribution is not permission. ..."), with cautionary styling driven by rightsIsCautionary; do NOT render lib/rights.rightsNote strings in the UI (they contain dash characters banned by the copy contract; the copy-deck line carries the same truth).
    3. Client-side gate: build a PartialAttribution from the stem context (sourceTrackId, stemId, stemType come from the selected stem/track; user supplies the rest) and drive the submit-disabled state + missing-field naming from missingAttributionFields (the same predicate the Rust gate enforces; integrity expressed twice, honestly).
    4. The 422 designed state (UI-SPEC, mandatory): catch IncompleteAttributionError; inline coral banner above the submit button with the copy-deck heading/body, the humanized missing list via humanizeAttributionField joined with commas; each missing field's label turns coral with a per-field "required for the credit" line; focus moves to the first missing field's input; the banner explicitly states nothing was created. Generic ApiError uses the standard in-overlay error banner instead.
    5. On success: whisper the copy-deck sample success line with the artist interpolated, return to the stems pane. (The credits veil from plan 11-04 will show the new row on next open; no cross-plan wiring needed.)
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>UI-03 complete: separate, browse, sample with honest client-side gating, the full designed 422 experience, and dash-free rendered strings; whole suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file → API | reference tracks uploaded as multipart Blobs |
| user form input → API | attribution strings and parsed time ranges |
| API responses → DOM | vibe prose, titles, artists, separator labels rendered in drawers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | attribution form inputs | mitigate | parseClock rejects malformed/negative ranges client-side; the server's attribution gate remains authoritative (client can only refuse to send, never forge) |
| T-11-12 | Spoofing/XSS | vibe prose + artist/title strings | mitigate | text nodes only; vibe renders as plain text paragraphs, never markup |
| T-11-13 | Repudiation | rights status honesty | mitigate | rights note always visible; cautionary styling for uncleared/unknown; no UI path hides the rights field |
| T-11-14 | Tampering | reference upload input | mitigate | accept="audio/*"; bytes travel multipart through the seam only |
| T-11-SC | Tampering | npm installs | mitigate | no installs in this plan |
</threat_model>

<verification>
- `cd web && npx vitest run` green (clock, humanize, both drawers, attribution incl. 422)
- `cd web && npx tsc --noEmit` and `npm run build` green
- UI-02 + UI-03 fully reachable from the dock inside the single surface
</verification>

<success_criteria>
- Reference drawer matches its Component Inventory row: vibe first, measurables mono, 5 bars, attach roles, non-cloning note (D-07, D-09)
- Stem drawer + attribution form deliver the complete sampling flow with in-drawer depth and back affordance (D-06)
- The 422 incomplete_attribution state is implemented exactly as designed, focus management included
- All rendered strings dash-free; copy comes from copy.ts
</success_criteria>

<output>
Create `.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-05-SUMMARY.md` when done
</output>
