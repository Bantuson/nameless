---
phase: 11-artistic-unified-ui-artistic-first-dark-single-surface-app-r
plan: 06
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04, 11-05]
files_modified:
  - web/src/**/*.css (motion + token sweep)
  - web/src/App.test.tsx
  - web/src/coreLoop.test.tsx
  - web/src/shell/*.tsx (a11y fixes as found)
  - web/src/overlays/*.tsx (a11y + state fixes as found)
  - web/src/constellation/*.tsx (a11y fixes as found)
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "The entire core loop (capture, see it understood, sample, credits) is operable keyboard-only, proven by an integration test"
    - "Every motion uses the ease/duration tokens and animates transform/opacity only; prefers-reduced-motion collapses everything to instant or 150ms crossfades"
    - "No scroll listeners, no raw-HTML injection props, no dash characters in UI source, no arbitrary stacking values, no pure black/white hex anywhere in the new UI"
    - "Every component in the UI-SPEC inventory has its loading, empty, error, and success states present"
    - "npx vitest run, npx tsc --noEmit, and npm run build are green as the phase gate"
  artifacts:
    - path: "web/src/coreLoop.test.tsx"
      provides: "Keyboard-only integration test of the full producer loop against MockNamelessApi"
      min_lines: 60
  key_links:
    - from: "web/src/coreLoop.test.tsx"
      to: "web/src/App.tsx"
      via: "renders the full shell via renderWithApi and drives it with userEvent.keyboard/tab"
      pattern: "renderWithApi"
---

<objective>
The polish pass: sweep the whole new surface against the Motion, Accessibility, and Anti-Slop contracts, prove the tutorial-free core loop keyboard-only, and run the phase gate.

Decision traceability: D-08 (the core loop must be completable without a tutorial and without a pointer), D-01/D-09 (the anti-slop guardrails and screenshot criterion are this phase's own success criterion), D-02/D-03 (final color/typography audit against the binding tables), D-06 (progressive disclosure verified: surface calm at rest, one overlay at a time).

Purpose: individual plans proved their components; this plan proves the phase as one coherent artistic surface.
Output: audited, fixed, fully green codebase + the end-of-phase human verification checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-UI-SPEC.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-CONTEXT.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-04-SUMMARY.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-05-SUMMARY.md
@web/src/copy.ts
@web/src/test/renderWithApi.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Motion + anti-slop + color/typography audit sweep</name>
  <files>web/src (CSS and components as violations are found)</files>
  <action>
    Audit every new file against the binding contracts and FIX violations in place. Work through these checklists explicitly, recording each finding + fix in the summary:
    1. Motion Contract: every transition/animation uses the tokens (ease token, 150/200/260/420/900ms set); only transform and opacity animate (no layout properties); the only infinite loop is the 2.4s analyzing breath; no uniform entrance animation applied to every element; whisper/overlay/landing durations match their table rows.
    2. Reduced motion: with the media query active, all transitions collapse to instant or at most 150ms crossfades; the breath becomes static + text badge; the scene renders one static frame; parallax off. Verify the tokens.css collapse block actually reaches every component (no hardcoded durations that bypass it; sweep for raw ms values in CSS and replace with tokens).
    3. Anti-slop guardrails (UI-SPEC list, D-01): no identical-tile card grids; no uppercase tracked eyebrows; skeletons match final shape and no spinner exists anywhere; empty states are composed copy with no fake rows; no glow or glassmorphism on DOM elements; corner radii only 8/12/full and nothing above 16px; shadows ink-tinted, max 24px blur, overlays only, never border+shadow decoration.
    4. Color audit: accent token usage appears ONLY in the five reserved places (landing CTA, dock Capture, record button, selected node halo, active job pulse); run `grep -rn "var(--accent" web/src --include='*.css'` and reconcile every line against that list, removing strays. No hex literals outside tokens.css. Amber only on sampling/attribution provenance. Coral only on error semantics.
    5. Typography audit: only the four sizes and two weights from the Typography table; mono only on musical metadata (key, tempo, LUFS, durations, time ranges, ids); no uppercase body copy; no gradient text.
    6. Dash sweep: remove every em/en dash character (U+2014/U+2013) from code, strings, and comments under the new UI directories (copy, shell, overlays, constellation, whispers, scene, capture); rephrase with periods, commas, or the word "to". Files kept from the old app that are not rendered as UI copy (api/ docs comments, lib/rights.ts notes) are exempt and out of this gate's scope.
  </action>
  <verify>
    <automated>cd web && bash -c "set -e; ! grep -rn \"addEventListener('scroll'\" src; ! grep -rn 'dangerouslySetInnerHTML' src; ! grep -rnP '[\x{2013}\x{2014}]' src/copy.ts src/shell src/overlays src/constellation src/whispers src/scene src/capture; ! (grep -rn 'z-index' src --include='*.css' | grep -v 'var(--z-'); ! grep -rniE '#(000|fff)([^0-9a-f]|$)|#(000000|ffffff)' src --include='*.css' --include='*.tsx'; echo GUARDRAILS-CLEAN" && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>All six checklists audited with findings fixed; every guardrail grep gate passes; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keyboard-complete core-loop proof + phase gate</name>
  <files>web/src/coreLoop.test.tsx, web/src/App.test.tsx, plus a11y fixes in shell/overlays/constellation as the test uncovers them</files>
  <behavior>
    - Test (the capstone, keyboard-only via userEvent.tab()/keyboard(), MockNamelessApi with simulateAnalysis, FakeRecorder, FakeSceneDriver, fake timers):
      1. Land, Tab to "Enter the studio", Enter: home appears
      2. Create a project through Start-a-project by typing + Enter: dock appears
      3. Tab to dock Capture, Enter: sheet opens with focus trapped inside; attach an audio file via the upload input, type the intent note, Enter on submit: sheet closes, focus returns to the dock button, the captured whisper is live
      4. Advance timers: the node resolves analyzed and the "Understood" whisper appears; Tab to the node button, Enter: Fragment veil opens; Escape closes and returns focus
      5. Open Stems from the dock, keyboard through to "Sample this stem", fill the attribution form (artist, times, rights) by keyboard, submit: sampled whisper appears
      6. Open Credits from the dock: the new credit row is present with the persistent attribution line
    - Test: every overlay announces via role="dialog" + accessible name; whispers live in an aria-live="polite" region; the skip link is first in tab order
    - Test: App renders no element with a positive tabindex anywhere (tab order is creation order)
  </behavior>
  <action>
    1. Write `web/src/coreLoop.test.tsx` walking the six steps above strictly through keyboard events; fix any component that blocks the path (missing focus management, unreachable control, wrong roles) in place. This test IS the D-08 acceptance: a first-time user completes capture, sees it understood, samples, and reads credits with one obvious next action at each step and no tutorial.
    2. Refresh `App.test.tsx` shell assertions if plan 11-04's HomeSurface changes drifted them.
    3. Run the full phase gate and record versions/output for the summary.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build</automated>
    <human-check>
      End-of-phase visual verification on a WebGL-capable browser (npm run dev, mock client):
      1. Landing shows the living Night Bloom nebula (drifting particles, ink-to-rose-to-amber ramp, slow 72 BPM luminance breath, pointer parallax); no template look, per D-01's screenshot criterion
      2. "Enter the studio" plays the 900ms pull-back + dim to the home surface; the scene stays alive behind the constellation
      3. Capture a fragment with the real microphone; the scene blooms once on success; the node appears, breathes, and resolves with key/tempo
      4. Walk references, stems + sampling (including submitting an intentionally incomplete attribution to see the 422 state), and credits
      5. Toggle OS reduced-motion: everything cuts to crossfades and the scene freezes to one frame
    </human-check>
  </verify>
  <done>Core loop proven keyboard-only in jsdom; full suite + typecheck + build green; human checklist delivered for the WebGL/mic paths that jsdom cannot exercise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole client surface | final sweep verifies the mitigations promised by plans 01-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering/XSS | all rendered API strings | mitigate | the raw-HTML-injection grep gate in Task 1 closes this phase-wide |
| T-11-15 | Elevation of Privilege | focus/keyboard traps | mitigate | core-loop test proves focus is trapped in overlays and returned, never lost to hidden layers |
| T-11-SC | Tampering | npm installs | mitigate | no installs in this plan; final lockfile identical to post-11-01 state except versions pinned then |
</threat_model>

<verification>
- Task 1 guardrail gates all pass (scroll/raw-HTML/dash/stacking/pure-hex)
- `cd web && npx vitest run && npx tsc --noEmit && npm run build` green (the phase gate; runnable-here work, not env-gated)
- Human checklist covers only what jsdom cannot: WebGL visuals, real mic, OS reduced-motion
</verification>

<success_criteria>
- Keyboard-complete core loop proven by test (D-08, Accessibility Contract)
- Motion, color, typography, and anti-slop contracts hold everywhere (D-01, D-02, D-09)
- Phase gate green; the phase's own failure condition ("templated in a screenshot") is checked by a human against the real scene
</success_criteria>

<output>
Create `.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-06-SUMMARY.md` when done
</output>
