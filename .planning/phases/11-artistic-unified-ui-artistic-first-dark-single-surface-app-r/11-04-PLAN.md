---
phase: 11-artistic-unified-ui-artistic-first-dark-single-surface-app-r
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - web/src/lib/constellationLayout.ts
  - web/src/lib/constellationLayout.test.ts
  - web/src/constellation/Constellation.tsx
  - web/src/constellation/FragmentListView.tsx
  - web/src/constellation/useProjectGraph.ts
  - web/src/constellation/constellation.test.tsx
  - web/src/shell/HomeSurface.tsx
  - web/src/overlays/FragmentVeil.tsx
  - web/src/overlays/FragmentVeil.test.tsx
  - web/src/overlays/CreditsVeil.tsx
  - web/src/overlays/CreditsVeil.test.tsx
  - web/src/api/MockNamelessApi.ts
  - web/src/api/createClient.ts
autonomous: true
requirements: [UI-04]

must_haves:
  truths:
    - "The home surface centers the fragment constellation: nodes colored by provenance, ringed by state, analyzed nodes labeled with note + key and tempo in mono"
    - "Every node is a real button; click/Enter opens the Fragment veil with note headline, badges, mono metadata, lineage line, and (for sampled fragments) the amber attribution block"
    - "While any node is captured/analyzing the graph polls every 5s; when a node resolves, a whisper says 'Understood: {key}, {tempo} BPM'"
    - "A quiet List toggle swaps the constellation for an accessible list of the same nodes with the same click targets"
    - "The Credits veil lists every sample row and 'Copy credits sheet' copies the markdown field; the 'attribution is not permission' line is always visible"
  artifacts:
    - path: "web/src/lib/constellationLayout.ts"
      provides: "Deterministic pure layout: same nodes+edges+seed, same sky"
      exports: ["layoutConstellation"]
    - path: "web/src/constellation/Constellation.tsx"
      provides: "SVG constellation layer: node buttons, halos, curved lineage hairlines, analyzing breath, empty state"
      min_lines: 100
    - path: "web/src/overlays/FragmentVeil.tsx"
      provides: "Fragment detail veil incl. sampled-fragment attribution block"
    - path: "web/src/overlays/CreditsVeil.tsx"
      provides: "Credits veil with copy-markdown action"
  key_links:
    - from: "web/src/constellation/useProjectGraph.ts"
      to: "web/src/api/NamelessApi.ts"
      via: "getProjectGraph(projectId) polled at 5s while any node is captured/analyzing"
      pattern: "getProjectGraph"
    - from: "web/src/constellation/Constellation.tsx"
      to: "web/src/overlays/OverlayContext.tsx"
      via: "node click opens {kind:'fragment', fragmentId}"
      pattern: "openOverlay\\(\\{\\s*kind: 'fragment'"
    - from: "web/src/overlays/FragmentVeil.tsx"
      to: "web/src/api/NamelessApi.ts"
      via: "getFragment + getSample when provenance is sampled"
      pattern: "getSample"
---

<objective>
Make the producer's material the living heart of home: the fragment constellation (SVG over the dimmed scene), the accessible List alternative, the Fragment veil, and the Credits veil. Together these complete UI-04 (graph + credits) inside the single surface.

Decision traceability: D-05/D-06 (constellation is layer 1 of the one surface; detail reveals on node intent), D-07 (the sky is summaries: notes and key/tempo, never raw data; the system whispers when it understands a fragment), D-08 (capture, then see it understood, is visible without any tutorial), D-03 (a constellation over the night scene, not a data table), D-09 (Constellation Contract table is binding).

Purpose: this is where "your material as a living constellation rather than a data table" (11-CONTEXT.md Specific Ideas) becomes real.
Output: constellation + list + both veils, all four component states each, polling with whispers, deterministic layout with tests; mock gains an opt-in analysis simulation so the dev loop shows capture resolving to key/tempo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-UI-SPEC.md
@.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-02-SUMMARY.md
@web/src/api/types.ts
@web/src/api/MockNamelessApi.ts
@web/src/api/fixtures.ts
@web/src/overlays/OverlayContext.tsx
@web/src/overlays/primitives/VeilShell.tsx
@web/src/whispers/WhisperContext.tsx
@web/src/shell/HomeSurface.tsx
@web/src/copy.ts
@web/src/lib/format.ts
@web/src/lib/rights.ts
@web/src/hooks/useAsyncData.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic constellation layout + mock analysis simulation</name>
  <files>web/src/lib/constellationLayout.ts, web/src/lib/constellationLayout.test.ts, web/src/api/MockNamelessApi.ts, web/src/api/createClient.ts</files>
  <behavior>
    - Test: layoutConstellation(nodes, edges, seed) is pure and deterministic: identical input yields identical positions; a different seed changes them
    - Test: positions land inside the unit viewBox with a margin; no two nodes closer than a minimum separation for up to 50 nodes
    - Test: a child node (parent_fragment_id set) is placed nearer its parent than the average inter-node distance (lineage reads spatially)
    - Test: MockNamelessApi WITHOUT the flag behaves exactly as before (existing MockNamelessApi.test.ts untouched and passing)
    - Test: MockNamelessApi with {simulateAnalysis:true}: a captured fragment appears as analyzed with a plausible key and tempo on the SECOND getProjectGraph read after capture
  </behavior>
  <action>
    1. `lib/constellationLayout.ts` (Constellation Contract layout row): `layoutConstellation(nodes: FragmentNode[], edges: GraphEdge[], seed: string): Map<Uuid, {x:number,y:number}>` in a normalized 0..1 space. Radial/organic placement: hash each fragment id with the seed (mulberry32-style PRNG) into angle + radius bands, bias children toward their parent's position, then run a few relaxation iterations to enforce minimum separation. Pure function, no randomness beyond the seeded PRNG, no React, no DOM.
    2. `MockNamelessApi.ts`: add an OPT-IN constructor option `{ simulateAnalysis?: boolean }` (default false so every existing adapter test passes unchanged; the api seam contract itself is untouched). When true, a fragment captured through capture() is promoted to state 'analyzed' with a deterministic plausible key (e.g. from a small key list hashed by id) and tempo when graph/fragment reads occur after the first post-capture read. This exists so the mock-driven dev loop demonstrates D-08's "capture, then see it understood" without the real worker.
    3. `createClient.ts`: pass `{ simulateAnalysis: true }` in mock mode. HTTP mode unchanged.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/lib/constellationLayout.test.ts src/api</automated>
  </verify>
  <done>Layout is deterministic, separated, lineage-aware, fully unit-tested; mock simulation is opt-in with all pre-existing api tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The constellation, the List alternative, and graph polling with whispers</name>
  <files>web/src/constellation/Constellation.tsx, web/src/constellation/FragmentListView.tsx, web/src/constellation/useProjectGraph.ts, web/src/constellation/constellation.test.tsx, web/src/shell/HomeSurface.tsx</files>
  <behavior>
    - Test: given a graph with human_recorded, sampled, and derived nodes, each renders its provenance core color class and its state ring class per the Color mapping table
    - Test: every node renders as a real button (role button, tabbable in creation order); Enter opens {kind:'fragment', fragmentId}
    - Test: an analyzed node shows its truncated note (24ch) and a "key" + "tempo BPM" mono label separated by a middle dot; captured/analyzing nodes show neither but carry the breath class ("listening" badge instead under mocked reduced motion)
    - Test: lineage edges render as curved paths; hovering/focusing an endpoint brightens the edge class
    - Test (fake timers, scripted api fake): while any node is captured/analyzing, getProjectGraph is called again after 5s; when a node flips to analyzed between polls, the whisper "Understood: {key}, {tempo} BPM" is queued and polling stops once no node is pending
    - Test: zero nodes renders the empty-sky whisper copy from copy.ts (heading + body), composed, no fake rows
    - Test: the List toggle swaps to FragmentListView showing id, note, kind, state, key/tempo rows with the same open-veil click target
  </behavior>
  <action>
    1. `useProjectGraph.ts`: hook over the injected api. Loads getProjectGraph(projectId); if any node state is 'captured' or 'analyzing', re-fetch every 5s (cleanup on unmount; Constellation Contract data row); diff consecutive graphs and fire the analyzed whisper (copy.ts) per newly analyzed node (D-07: the system narrates). Expose {graph, loading, error, refresh}. refresh() is called by overlay flows elsewhere via re-mount or context if needed; keep it simple.
    2. `Constellation.tsx`: an SVG (viewBox mapped from the layout's unit space) positioned as the surface centerpiece over the dimmed scene (the SVG layer decision in the Constellation Contract keeps it accessible and jsdom-testable, NOT WebGL). Node anatomy per contract: 12px core disc + 28px soft halo (SVG radial gradient, not a DOM glow); core color by provenance (var(--text) warm-white / var(--amber) sampled / var(--text-dim) derived; the ai_generated cool tone stays reserved); state ring: captured/analyzing dim + 2.4s opacity breath 0.5 to 0.9 (CSS animation; under reduced motion: static 0.7 opacity + a textual "listening" badge from copy.ts); analyzed: full luminance + note (truncated 24ch via notePreview, Body size) + key and tempo beneath in JetBrains Mono 13px, middle-dot separated; placed: lit lineage edge. Selected node (its veil open) gets the accent halo (Color reserved list no. 4). Hover/focus lifts halo luminance 20%. Nodes are `<button>` elements inside foreignObject or SVG `<a>`-pattern wrappers; whichever is chosen, role/tab/focus-ring behavior must pass the tests and the focus ring must be visible (--focus token).
    3. Edges: 1px curved hairlines (quadratic paths) at the strong border token, brightening on endpoint hover/selection.
    4. `FragmentListView.tsx`: the a11y/testing-equivalent quiet list (Constellation Contract list row): same node data (short id in mono, note, kind, state, key/tempo), each row a button opening the same veil.
    5. `HomeSurface.tsx`: replace the empty-sky region from plan 11-01: render the constellation (or FragmentListView per a quiet "List"/"Sky" toggle, top-right of the surface, label text only, no accent); when the graph has zero nodes render the empty state (copy.ts heading "Your first idea goes here" + body) with the visual arrow of intent pointing at the dock's Capture (D-08: the only accent on screen stays the Capture action).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run src/constellation</automated>
  </verify>
  <done>The constellation renders per contract with full keyboard access, polling + whisper narration works on fake timers, list alternative is equivalent, empty state is composed copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fragment veil + Credits veil</name>
  <files>web/src/overlays/FragmentVeil.tsx, web/src/overlays/FragmentVeil.test.tsx, web/src/overlays/CreditsVeil.tsx, web/src/overlays/CreditsVeil.test.tsx</files>
  <behavior>
    - Test: FragmentVeil fetches getFragment and renders the note as the headline; kind/provenance/state as quiet badges; duration in mono; loading shows the skeleton, error shows the generic error copy with retry
    - Test: a parented fragment shows the lineage line ("derived from" + parent short id)
    - Test: a sampled fragment additionally fetches getSample and renders source title/artist, stem type, time range (mono, dash-free), and the rights badge in amber; rights caution styling for uncleared/unknown via rightsIsCautionary
    - Test: CreditsVeil renders one row per credit sample (title, artist, amber stem-type badge, mono time range, rights badge) plus the persistent attribution-is-not-permission line from copy.ts
    - Test: "Copy credits sheet" writes the credits.markdown string via an injected clipboard function (test double asserts the exact payload); empty credits renders the copy-deck empty state
  </behavior>
  <action>
    1. Complete `FragmentVeil.tsx` (replacing the plan 11-02 shell) inside VeilShell: fetch `getFragment(fragmentId)`; key/tempo come from an additional `getProjectGraph(detail.project_id)` lookup of this node (FragmentDetail itself carries no key/tempo; the graph node does). Layout per Component Inventory: note as the headline (Body scale up to Heading), badges row, `key`, `tempo BPM`, duration in JetBrains Mono separated by middle dots, lineage line when parent_fragment_id is set. When provenance is 'sampled', also `getSample(fragment)` and render the full attribution block with the amber rights badge (Color provenance mapping; D-09). NotFoundError renders the copy-deck notFound state. All four component states designed.
    2. Complete `CreditsVeil.tsx` inside VeilShell: `getCredits(activeProjectId)`; rows per contract; "Copy credits sheet" button calls an injectable `writeClipboard: (text:string)=>Promise<void>` prop defaulting to `navigator.clipboard.writeText` (DI for jsdom tests); on success whisper a short confirmation; the persistent rights line renders under the rows in muted text. Empty state from copy.ts ("No samples used yet"). No em or en dash characters in any string this task renders or writes into source files.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>Both veils complete with all four states; sampled fragments show attribution + amber rights; credits copy the exact markdown; whole suite + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API responses → DOM | notes, titles, artists, markdown are user/API strings rendered on the surface |
| app → clipboard | credits markdown written to the system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Spoofing/XSS | notes and attribution strings in constellation/veils | mitigate | React text nodes only; SVG text content, never markup injection; no raw-HTML props (gated in 11-06) |
| T-11-09 | Information Disclosure | clipboard write | accept | user-initiated copy of the user's own credits sheet; no read access requested |
| T-11-10 | Denial of Service | 5s polling loop | mitigate | polls only while nodes are pending; interval cleared on unmount and when all nodes settle |
| T-11-SC | Tampering | npm installs | mitigate | no installs in this plan |
</threat_model>

<verification>
- `cd web && npx vitest run` green (layout, constellation, polling, both veils)
- `cd web && npx tsc --noEmit` and `npm run build` green
- UI-04 fully reachable: graph via constellation/list, credits via dock veil
</verification>

<success_criteria>
- The constellation matches the Constellation Contract row-for-row (anatomy, colors, edges, interaction, breath, list alternative, empty state)
- Deterministic layout proven by tests: same input, same sky
- Fragment + Credits veils complete UI-04 with all component states designed
- Polling narrates analysis completion through whispers (D-07, D-08)
</success_criteria>

<output>
Create `.planning/phases/11-artistic-unified-ui-artistic-first-dark-single-surface-app-r/11-04-SUMMARY.md` when done
</output>
